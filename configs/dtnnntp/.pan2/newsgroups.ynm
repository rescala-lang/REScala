# Permissions: y means posting ok; n means posting not okay; m means moderated.
# Since almost all groups allow posting, Pan assumes that as the default.
# Only moderated or no-posting groups are listed here.
