import rescala.engines.EngineImpl
import rescala.parrp.{Backoff, ParRPSpores, ParRP}

//package object rescala extends EngineImpl[ParRPSpores.type, ParRP](ParRPSpores, new ParRP(backOff(new Backoff)))
