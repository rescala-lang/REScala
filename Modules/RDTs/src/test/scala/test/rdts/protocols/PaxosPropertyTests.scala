package test.rdts.protocols

val a = 0
//import rdts.syntax.LocalReplicaId
//
//class PaxosPropertyTests {
//
//  val id1 = LocalReplicaId.gen()
//  val id2 = LocalReplicaId.gen()
//  val id3 = LocalReplicaId.gen()
//
//  enum Action(val code: Int):
//    case Upkeep   extends Action(0)
//    case Prepare  extends Action(1)
//    case Accept   extends Action(2)
//    case Accepted extends Action(3)
//
//  val ids = List(id1, id2, id3)

// action probabilities

// -> generate list of actions
// -> enrich actions with parameters
// -> compress action list by merging subsets

//}
